# 2026-03-24

## 今日讨论与执行事项

- 确认产品方向，采用“原生 SwiftUI macOS App + Python Worker 后端”的方案 B。
- 完成 SwiftUI 工作台主体结构，包含 `Translate`、`Library`、`Review Queue`、`Settings` 四个入口。
- 保留 Python 翻译管线作为业务核心，通过 worker 协议承接健康检查、翻译、文章读取、保存、导出等能力。
- 完成 Review 工作台的预览、Markdown 编辑、Split 校对、导出 HTML、复制富文本等流程。
- 修复界面细节问题：
  - Translate 任务日志改为倒序显示新消息
  - Library/Review 内容区域扩大
  - Split 视图改为上下布局
  - 文章标题移动到视图切换栏上方
  - 修复预览中异常 token 泄漏的问题
- 处理 Python 3.13 兼容性问题，为 `googletrans/httpx` 补充 `cgi` 兼容 shim。
- 在命令行下验证核心 Python 管线，测试文章：
  - `https://suggger.substack.com/p/the-architecture-of-not-bad-decoding`
- 讨论应用图标方向，确定偏好的概念为 `Ink Window`：
  - 一侧是钢笔/哥特字形笔触
  - 一侧是毛笔水墨形态
  - 整体使用琥珀与深色流光质感

## 验证结果

- `swift build` 通过
- `swift test` 通过
- `python3 -m unittest discover -s tests -v` 通过
- `./build_swiftui_app.sh` 可生成 `dist/博文翻译助手.app`
- 命令行翻译成功产出文章目录：
  - `articles/2026-03-24-the-architecture-of-not-bad-decoding/`

## 结论

- 原生 macOS 版本已经具备第一版可运行骨架，核心流程可用。
- Python CLI 管线已恢复并可正常处理真实文章。
- 仓库后续增加 `log/` 作为项目复盘记录目录；运行时日志继续使用 `logs/`。

## 后续约定

- 后续代码更新默认执行 git 提交并推送到远程仓库。
- 提交时区分“项目复盘记录”和“运行日志”，避免把生成型日志文件纳入版本控制。
